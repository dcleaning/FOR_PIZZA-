// scripts.js
function setLanguage(language) {
    if (language === 'ua') {
      // Логика для переключения на украинский
      document.querySelector('.lang-switch').textContent = 'Українська';
      // Здесь вы можете добавить больше логики для смены текста на странице
    } else if (language === 'ru') {
      // Логика для переключения на русский
      document.querySelector('.lang-switch').textContent = 'Русский';
      // Здесь вы можете добавить больше логики для смены текста на странице
    }
  }
  